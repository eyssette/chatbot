# https://taskfile.dev

version: '3'

tasks:
  push:
    vars:
      GIT_REPO_NAMES: [forge, github]
    cmds:
      - for: { var: GIT_REPO_NAMES }
        cmd:  git push {{.ITEM}} && git push {{.ITEM}} --tags